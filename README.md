# lyric-generator-rnn
A deep learning project that generates song lyrics using RNN (LSTM) trained on text data, with a user-friendly Streamlit interface and adjustable creativity using temperature sampling.
